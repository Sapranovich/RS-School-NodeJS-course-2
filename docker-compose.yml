# Use postgres/example user/password credentials
version: '3.1'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: example
    ports:
      - 5432:5432

  node-web-app:
    image: node-js-task6
    restart: always
    ports:
      - 4000:4000
